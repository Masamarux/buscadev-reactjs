import styled from 'styled-components';

import { COLORS } from '../../globalStyles';

export const AlertsStyle = styled.div`
  .Alert{
    color: inherit !important;
    background-color: ${COLORS.powder_blue};
    text-align: center;
  }
`;